#!ipxe
ifopen net0
dhcp net0
set opensuse http://download.opensuse.org/distribution/openSUSE-current/repo/oss
kernel ${opensuse}/boot/i386/loader/linux 
initrd ${opensuse}/boot/i386/loader/initrd
imgargs linux install=${opensuse}
boot ||
echo Booting failed, dropping to shell
shell
